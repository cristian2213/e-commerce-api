export enum Roles {
  CUSTOMER = 'customer',
  ADMIN = 'admin',
  DEALER = 'dealer',
}

export const RolesKeys: string[] = Object.keys(Roles);

export const RolesValues: string[] = Object.values(Roles);
